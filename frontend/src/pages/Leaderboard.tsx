import { useQuery } from "@tanstack/react-query";
import { Crown, Flame, Medal, Trophy } from "lucide-react";
import { apiGet } from "@/lib/api";
import type { LeaderboardEntry } from "@/lib/types";

const rankColors = ["#ffcc00", "#a8b0bd", "#bd7b4b"];

export default function Leaderboard() {
  const query = useQuery({ queryKey: ["leaderboard"], queryFn: () => apiGet<LeaderboardEntry[]>("/leaderboard"), retry: false });
  const entries = query.data ?? [];
  const podium = entries.slice(0, 3);
  return <div className="mx-auto max-w-[1200px]" data-testid="leaderboard-page"><div className="mb-10 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="mb-3 text-[10px] font-bold uppercase tracking-[0.28em] text-[#ff3b30]" data-testid="leaderboard-eyebrow">Competition / consistency</p><h1 className="font-heading text-5xl font-black uppercase leading-none tracking-tight" data-testid="leaderboard-title">The arena.</h1><p className="mt-4 max-w-lg text-sm leading-relaxed text-[#8a8a8e]" data-testid="leaderboard-subtitle">Consistency is a team sport. See who is showing up and set the next benchmark.</p></div><div className="flex items-center gap-2 text-xs uppercase tracking-[0.16em] text-[#8a8a8e]" data-testid="leaderboard-season-status"><span className="size-2 rounded-full bg-[#ff3b30]" />Live rankings</div></div><section className="mb-6 grid gap-px border border-[#262626] bg-[#262626] md:grid-cols-3" data-testid="podium-grid">{podium.length === 0 ? <div className="col-span-3 bg-[#141414] p-10 text-sm text-[#8a8a8e]" data-testid="empty-leaderboard-state">Be the first name on the board. Complete a habit to start scoring.</div> : podium.map((entry, index) => <div className={`relative bg-[#141414] p-7 ${index === 0 ? "md:-translate-y-3 md:pt-10" : ""}`} key={entry.rank} data-testid={`podium-entry-${entry.rank}`}><div className="mb-10 flex items-center justify-between"><span className="font-heading text-5xl font-black" style={{ color: rankColors[index] }}>{String(entry.rank).padStart(2, "0")}</span>{index === 0 ? <Crown size={22} className="text-[#ffcc00]" /> : <Medal size={21} className="text-[#555]" />}</div><div className="flex items-end justify-between"><div><p className="font-heading text-2xl font-black uppercase" data-testid={`podium-name-${entry.rank}`}>{entry.name}</p><p className="mt-2 text-[10px] uppercase tracking-[0.18em] text-[#8a8a8e]">{entry.streak} day streak</p></div><p className="font-mono text-2xl font-bold text-[#007aff]" data-testid={`podium-score-${entry.rank}`}>{entry.score}<span className="ml-1 text-[10px] text-[#8a8a8e]">pts</span></p></div></div>)}</section><section className="border border-[#262626] bg-[#141414]" data-testid="leaderboard-table-panel"><div className="flex items-center justify-between border-b border-[#262626] p-6"><div className="flex items-center gap-3"><Trophy size={18} className="text-[#ffcc00]" /><h2 className="font-heading text-2xl font-black uppercase">Full rankings</h2></div><span className="font-mono text-xs text-[#8a8a8e]" data-testid="leaderboard-count">{entries.length} players</span></div><div className="overflow-x-auto"><table className="w-full min-w-[600px] text-left"><thead className="border-b border-[#262626] text-[10px] uppercase tracking-[0.18em] text-[#555]"><tr><th className="px-6 py-4">Rank</th><th className="px-6 py-4">Player</th><th className="px-6 py-4">Consistency</th><th className="px-6 py-4">Streak</th><th className="px-6 py-4">Completed</th></tr></thead><tbody>{entries.map((entry) => <tr className="border-b border-[#262626] transition-colors hover:bg-[#191919]" key={entry.rank} data-testid={`leaderboard-row-${entry.rank}`}><td className="px-6 py-5 font-mono text-sm text-[#8a8a8e]">{String(entry.rank).padStart(2, "0")}</td><td className="px-6 py-5 font-semibold" data-testid={`leaderboard-player-${entry.rank}`}>{entry.name}</td><td className="px-6 py-5 font-mono text-sm text-[#007aff]">{entry.score} pts</td><td className="px-6 py-5 text-sm text-[#8a8a8e]"><span className="inline-flex items-center gap-2"><Flame size={14} className="text-[#ff3b30]" />{entry.streak}d</span></td><td className="px-6 py-5 font-mono text-sm text-[#8a8a8e]">{entry.completed}</td></tr>)}</tbody></table></div></section></div>;
}